{
    "name": "hng/ticket-app-twig",
    "description": "HNG Stage 2 - Ticket Management App using Twig templates",
    "type": "project",
    "require": {
        "twig/twig": "^3.14",
        "php": ">=8.0"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "scripts": {
        "serve": "php -S localhost:8000 public/index.php"
    }
}